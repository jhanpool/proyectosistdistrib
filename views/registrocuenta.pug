<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
        link(rel="stylesheet", href="/css/acceso.css")

    body
        .container
            .d-flex.justify-content-center.h-100
                .card
                    .card-header.text-center
                        h2 Registro

                    .card-body
                        form(method="POST" action="/registrocuenta")
                            .input-group.form-group
                                .input-group-prepend
                                    span.input-group-text
                                        i.fa.fa-envelope-o
                                input.form-control(type="text" id="correo" name="correo" value=correo placeholder="Email")
                                //required
                            
                            .input-group.form-group
                                .input-group-prepend
                                    span.input-group-text
                                        i.fa.fa-key
                                input.form-control(type="password" id="clave" name="clave" value=clave placeholder="Clave")
                                //required
                            
                            .form-group
                                input.btn.float-right.acceso_btn(type="submit" value="Registrar")

                        //IF PARA EL CONTROL DE ERRORES O VALIDACIONES
                        //if errores
                            each error in errores
                                .alert.alert-danger(role="alert")=error.message

                        //IF CON FLASH PARA EL CONTROL DE ERRORES O VALIDACIONES
                        if locals.mensajes
                            each categoria in Object.keys(locals.mensajes)
                                each error in mensajes[categoria]
                                    //.alert.alert-danger.alert-dismissible.fade.show(role="alert")
                                    .alert(class=`${categoria}` role="alert")
                                        strong=error
                                        button.close(type="button" data-dismiss="alert" aria-label="Close")
                                            span(aria-hidden="true") ×

                    .card-footer
                        .d-flex.justify-content-center.links
                            | ¿Ya cuentas con un registro?
                            a(href="/iniciarsesion") Inicia Sesión

        include partials/footer