<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
        link(rel="stylesheet", href="/css/acceso.css")

    body
        .container
            .d-flex.justify-content-center.h-100
                .card
                    .card-header.text-center
                        h2 Iniciar Sesión

                    .card-body
                        form(method="POST" action="/iniciarsesion")
                            .input-group.form-group
                                .input-group-prepend
                                    span.input-group-text
                                        i.fa.fa-envelope-o
                                input.form-control(type="email" id="correo" name="correo" value=correo placeholder="Email")
                            
                            .input-group.form-group
                                .input-group-prepend
                                    span.input-group-text
                                        i.fa.fa-key
                                input.form-control(type="password" id="clave" name="clave" value=clave placeholder="Clave")
                            
                            .form-group
                                input.btn.float-right.acceso_btn(type="submit" value="Iniciar")
                        br
                        br
                        //if error
                        if locals.mensajes
                            each categoria in Object.keys(locals.mensajes)
                                each error in mensajes[categoria]
                                    .alert.alert-warning.alert-dismissible.fade.show(role="alert")
                                        strong=error
                                        button.close(type="button" data-dismiss="alert" aria-label="Close")
                                            span(aria-hidden="true") ×

                    .card-footer
                        .d-flex.justify-content-center.links
                            | ¿No cuentas con usuario?
                            a(href="/registrocuenta") Registro Aquí
                        .d-flex.justify-content-center.links
                            | ¿Olvidaste tu clave?
                            a(href="/restablecer") Recupera Aquí

        include partials/footer